{
  "name": "@streamdota/gsi-parser",
  "version": "1.0.0",
  "description": "Library to parse GSI events from raw data",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "repository": "https://github.com/michaelketzer/streamdota-gsi-parser",
  "author": "Michael Ketzer",
  "license": "MIT",
  "private": false,
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^26.0.16",
    "jest": "^26.6.3",
    "prettier": "^2.2.1",
    "ts-jest": "^26.4.4",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.1.2"
  },
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "tslint -p tsconfig.json",
    "test": "jest --config jestconfig.json",

    "prepare" : "npm run build",
    "prepublishOnly" : "npm test && npm run lint",
    "preversion" : "npm run lint",
    "version" : "npm run format && git add -A src",
    "postversion" : "git push && git push --tags"
  },
  "files": [
    "lib/**/*"
  ],
  "keywords": ["Dota2", "GSI", "Parser"]
}
